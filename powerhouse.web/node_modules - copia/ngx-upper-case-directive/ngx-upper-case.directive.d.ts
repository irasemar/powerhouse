export declare class NgxUpperCaseDirective {
    upperCase: string;
    private getCaret(el);
    private setCaret(el, start, end);
    private dispatchEvent(el, eventType);
    private convertValue(el, value);
    onBlur(el: any, value: string): void;
    onInput(el: any, value: string): void;
}
